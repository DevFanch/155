const $form = document.getElementById('user-contact')
$form.addEventListener('submit', function (e) {
    e.preventDefault()
    console.log(this.querySelectorAll('input'));
    // Faire une boucle sur la collection

        // Sur chaque element => test 
        // si el.id === 'email' alors
            // je test
        // sinon

    // Fin de la boucle

    // Test est-ce que j'ai rencontré des erreurs ??
    // Sinon j'enregistre
})